import javax.swing.*;
import java.awt.*;

public class GameView extends JPanel {
    GameView() {
        this.setBackground(new Color(-60672));
        // Selasi add your ui code here
        // We'll need to change it later to be launched by clicking on a game tile
        // But you can go ahead and build out your ui here and we can change it later
    }

    public void UpdateView() {}
}
